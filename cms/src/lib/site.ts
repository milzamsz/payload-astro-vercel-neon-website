/**
 * Branding constants for the CMS admin + SEO plugin. Public-facing site content
 * lives in the Astro `web/` app and the Payload globals; this is just the
 * admin-panel identity and SEO title suffix.
 */
export const branding = {
  siteName: 'Northvale Studio',
  shortName: 'Northvale',
}
